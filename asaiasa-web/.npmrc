# Fix for Rollup optional dependencies issue on Vercel
# This ensures all optional dependencies are installed
optional=false
fund=false
audit=false

# Use exact versions to avoid dependency resolution issues
save-exact=true

# Prefer offline packages
prefer-offline=true
