# ===========================================
# ASAiASA Frontend Environment Configuration
# ===========================================

# API Configuration
# Backend API base URL - change this to your backend server URL
VITE_API_BASE_URL=http://localhost:8080
# For production, use your deployed backend URL:
# VITE_API_BASE_URL=https://talent-atmos.pmaw.net

# API timeout in milliseconds
VITE_API_TIMEOUT=30000

# Google OAuth Configuration
# Get these from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=295739018389-0v5a3v5787ar66obg9ocmdrmi68g1897.apps.googleusercontent.com

# Application Configuration
VITE_APP_NAME=ASAiASA
VITE_APP_DESCRIPTION=Find ESG Events and Jobs
VITE_ENVIRONMENT=development

# Frontend URLs
# Base URL for your frontend application
VITE_BASE_URL=http://localhost:3000
# Admin panel URL (if different)
VITE_ADMIN_URL=https://ta-mgmt-cons.netlify.app

# Internationalization Settings
VITE_DEFAULT_LOCALE=en
VITE_SUPPORTED_LOCALES=en,th

# Feature Flags
# Enable/disable features
VITE_ENABLE_GOOGLE_AUTH=true
VITE_ENABLE_RECOMMENDATIONS=true
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_SEARCH=true

# Cookie Configuration
# For local development
VITE_COOKIE_DOMAIN=localhost
VITE_COOKIE_SECURE=false
VITE_COOKIE_SAME_SITE=lax

# For production
# VITE_COOKIE_DOMAIN=.yourdomain.com
# VITE_COOKIE_SECURE=true
# VITE_COOKIE_SAME_SITE=strict

# Development/Debug Settings
VITE_DEBUG_MODE=false
VITE_LOG_LEVEL=info

# ===========================================
# Backend Environment Variables (for reference)
# These are used by the Go backend server
# ===========================================

# Database Configuration
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/asaiasa?sslmode=disable&TimeZone=Asia%2FBangkok

# Server Configuration
# APP_PORT=8080

# JWT Configuration
# JWT_SECRET=your-jwt-secret-key-here

# Google OAuth (Backend)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Environment
# ENVIRONMENT=development

# CORS Configuration
# CORS_ORIGIN_URL=http://localhost:3000,http://localhost:5173

# External URLs
# BASE_EXTERNAL_URL=http://localhost:3000
# ADMIN_EXTERNAL_URL=http://localhost:3001

# Cookie Configuration (Backend)
# COOKIE_DOMAIN=localhost

# AWS S3 Configuration (if using file uploads)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET_NAME=your-bucket-name

# Redis Configuration (if using caching)
# REDIS_URL=redis://localhost:6379

# Elasticsearch Configuration (if using search)
# ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=your-password

# SMTP Configuration (for emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_MAIL=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ===========================================
# Setup Instructions
# ===========================================

# 1. Copy this file to .env in the same directory
# 2. Update the VITE_API_BASE_URL to match your backend server
# 3. Configure Google OAuth credentials if using authentication
# 4. Adjust other settings based on your environment
# 5. For production, update security settings (HTTPS, secure cookies, etc.)

# ===========================================
# Important Notes
# ===========================================

# - All VITE_ prefixed variables are available in the frontend code
# - Never commit sensitive credentials to version control
# - Use different configurations for development, staging, and production
# - The backend must be configured with matching CORS settings
# - Google OAuth requires proper domain configuration in Google Cloud Console
